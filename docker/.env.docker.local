# --- Flags de arranque ---
WAIT_FOR_DB=true                
RUN_MIGRATIONS=true              
RUN_SEEDERS=true            
RUN_BUILD_ASSETS=false           
CACHE_BOOTSTRAP=false            

# --- Parámetros del worker de colas ---
QUEUE_SLEEP=3
QUEUE_TRIES=3
QUEUE_MAX_JOBS=1000
QUEUE_QUEUES=sofia-validation,default

# --- WebSockets / Reverb ---
REVERB_SERVER_HOST=0.0.0.0
REVERB_SERVER_PORT=8080

# --- Vite ---
VITE_PORT=5173                   

# --- Overrides para el build Node/PHP ---
APP_ENV=local
APP_DEBUG=true
NODE_ENV=development
BUILD_ENV=local