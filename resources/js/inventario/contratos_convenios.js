/**
 * Contratos-convenios.js - Funcionalidades específicas para contratos/convenios
 * Utiliza inventario-common.js para funcionalidades compartidas
 */

// Usamos las funciones comunes directamente sin wrappers específicos
document.addEventListener('DOMContentLoaded', () => {
    // Configurar funciones específicas de contratos
    window.configurarContratos();
});